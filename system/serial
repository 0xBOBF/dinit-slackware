# Start the serial port setup script

type = scripted
command = /etc/dinit/system/scripts/serial.sh start
stop-command = /etc/dinit/system/scripts/serial.sh stop
restart = false
depends-on = tempfiles
